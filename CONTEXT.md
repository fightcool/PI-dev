# PI-dev 个人远程开发环境

PI-dev 是以 Pi 为主要编码 Agent、面向个人使用习惯的远程开发工作台。渠道管理服务于工作台中的编码过程。

## Language

**编码引擎**：负责组织上下文、调用模型、执行工具并维护对话的能力主体。本项目以 Pi 为主，保留既有 DSH 路径。
_Avoid_：把模型品牌或供应商称为编码引擎。

**渠道**：用户可以选择的一项模型访问来源，关联协议端点、授权和可用模型。同一供应商可以提供多个渠道。
_Avoid_：把渠道等同于模型、单把密钥或整个 Agent。

**协议端点**：渠道内使用某一种模型调用协议的服务入口。

**账户**：供应商侧承担资金余额或结算归属的主体，一个账户可能关联多个渠道或密钥。
_Avoid_：把每把密钥视为独立余额账户。

**凭据引用**：对某项授权材料的标识，不包含需要展示给用户的密钥正文。

**渠道绑定**：一次对话或调用所选择的渠道、端点、凭据和模型的组合。
_Avoid_：把界面刚选中的待生效选项称为实际使用渠道。

**绑定版本**：对话绑定每次变更递增的序号，用于拒绝旧回执覆盖新状态。

**配置版本**：渠道档案与默认值每次变更递增的序号，用于检测外部修改与陈旧提交。

**待生效选择**：已受理但尚未在安全边界（本轮结束）应用的渠道选择；期间在途请求与工具仍用原绑定。

**热切换**：在保留当前编码对话的情况下，使后续可安全执行的模型调用采用新的渠道绑定。
_Avoid_：把已发出的请求改道，或通过重启整个工作台实现普通切换。

**项目默认渠道**：新对话在项目中采用的默认选择；当前对话可以具有自己的绑定。
_Avoid_：把切换当前项目解释为修改所有对话的全局授权。

**对话**：持续承载编码目标、交互历史和上下文的对象，可以包含多次运行。

**运行**：一次被接受的编码执行过程，可以包含多次模型请求和工具调用。
_Avoid_：把一轮界面操作、一次模型请求和整个对话混为一谈。

**估算费用**：按照记录的模型用量和适用价格规则计算的费用估计。
_Avoid_：将其称为供应商最终扣费或账户余额。

**账户余额**：供应商报告的账户资金或额度状态，具有特定单位和查询时点。
_Avoid_：使用本地Token统计推算后标为实际余额。

**密钥配额**：供应商对某项密钥授权设置的可用量或限制，可能与账户余额分别约束调用。

**DEV-CON**：PI-dev 管理功能的名称，核心用途是渠道配置、切换和相关用量/账户信息管理。
_Avoid_：将其预设为必须独立部署的应用，或外部 Agent 聚合平台。
