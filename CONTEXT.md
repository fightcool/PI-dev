# PI-dev 个人远程开发环境

PI-dev 是以 Pi 为主要编码 Agent、面向个人使用习惯的远程开发工作台。模型调用统一经**一个网关**接入，
网关配置与用量口径见 [docs/NEWAPI-GATEWAY.md](docs/NEWAPI-GATEWAY.md)。

## Language

**编码引擎**：负责组织上下文、调用模型、执行工具并维护对话的能力主体。本项目以 Pi 为主，保留既有 DSH 路径。
_Avoid_：把模型品牌或供应商称为编码引擎。

**网关**：本实例**唯一**的模型调用入口，由一个接口地址（baseUrl）、一把密钥和一份模型清单构成。
对用户不存在「服务商/渠道」这个概念——多个入口正是被移除的旧模型。
_Avoid_：把网关等同于模型品牌、等同于某一台网关软件（NewAPI 只是其中一种实现）。

**网关地址（baseUrl）**：模型请求与账单查询的共同站点根；账单接口由它派生（剥掉 `/v1`、`/api/...`）。
_Avoid_：把带 `/v1` 的完整地址与站点根混用（会影响账单查询路径）。

**模型清单**：网关自己公布的可用模型集合（`/models`）。读取是**合并式**的：只新增不删除。

**网关自报用量**：网关自己的账单接口报出的累计已用与（可能没有的）额度，已换算成 USD。
_Avoid_：与「估算费用」相加，或当作实时计费（网关账单是异步统计的，滞后数秒）。

**估算费用**：按照记录的模型用量和适用的价目表（`models.json` 的 cost）计算的费用估计。
_Avoid_：称其为网关最终扣费。

**重复接入**：除网关之外，还有配置指向**同一个站点**（例如同一台网关被登记两次）。
界面只提示并可一键删除，不自动删。

**对话**：持续承载编码目标、交互历史和上下文的对象，可以包含多次运行。

**运行**：一次被接受的编码执行过程，可以包含多次模型请求和工具调用。
_Avoid_：把一轮界面操作、一次模型请求和整个对话混为一谈。

**系统资源**：宿主与应用的只读资源快照（CPU、内存、磁盘、unit 占用），带来源标签，读不到的字段为空而不是 0。

**用量历史**：按逐请求记录持久化的用量聚合视图，可跨服务商、项目、模型、来源与时间分组。
_Avoid_：把它当成供应商的实际扣费或账户余额。

**估算费用**：按照记录的模型用量和适用价格规则计算的费用估计。
_Avoid_：将其称为供应商最终扣费或账户余额。

**DEV-CON**：PI-dev 管理功能的名称，现含网关配置、网关用量、用量历史与运维诊断。
_Avoid_：将其预设为必须独立部署的应用，或外部 Agent 聚合平台。
