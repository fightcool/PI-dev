/*
 * 🍞 AI Breadcrumb Navigation — @COUPLED=synthetic regression runner.
 * @COUPLED ./message-list-window-test.mjs
 * @WHY Keep the documented lazy-window test entry without starting an agent
 * server. The synthetic browser fixture verifies bounded history and search;
 * the old assertions required one placeholder per row and unbounded search DOM.
 */
import "./message-list-window-test.mjs";
